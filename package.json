{
  "name": "ecuprogramsimulation",
  "version": "0.1.0",
  "description": "Simulate specific modules in ECU program, especially for the pure math works.",
  "main": "./app_start/main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "electron-packager ./ ECUSIM --app-version=0.2.0 --platform=win32 --arch=ia32 --icon=./styles/app_icon/app.ico --ignore=node_modules/electron-*"
  },
  "keywords": [
    "ECU",
    "Simulation"
  ],
  "author": "Akai",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^9.1.0",
    "electron-prebuilt": "^1.4.13",
    "grunt-electron-installer": "^2.1.0"
  },
  "dependencies": {
    "echarts": "^3.7.2"
  }
}
